<% provide :title, "Reset password" %>

<div class="min-h-screen bg-gray-50 flex items-center justify-center px-4 py-12">
  <div class="w-[420px] max-w-full rounded-[14px] bg-white p-8 shadow-[0_10px_30px_rgba(0,0,0,0.08)]">
    
    <!-- Brand -->
    <div class="mb-6 flex items-center justify-center gap-2">
      <svg xmlns="http://www.w3.org/2000/svg" width="26" height="26" viewBox="0 0 24 24" fill="currentColor">
        <circle cx="8" cy="12" r="4"></circle>
        <circle cx="16" cy="12" r="4"></circle>
      </svg>
      <span class="text-[22px] font-semibold text-gray-900">tokenlab</span>
    </div>

    <h1 class="text-center text-[20px] font-semibold text-gray-900">Reset your password</h1>
    <p class="mt-1 text-center text-[13px] text-gray-500">Enter a new password below</p>

    <!-- Errors -->
    <% if resource.errors.any? %>
      <div class="mb-4 rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">
        <p class="font-medium">Please fix the following:</p>
        <ul class="list-disc pl-5">
          <% resource.errors.full_messages.each do |m| %>
            <li><%= m %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <!-- Form -->
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }, data: { turbo: true }) do |f| %>
      <%= f.hidden_field :reset_password_token %>

      <div class="mt-6 space-y-4">
        <div>
          <%= f.label :password, "New password*", class: "block text-[13px] font-medium text-gray-700" %>
          <% if @minimum_password_length %>
            <span class="text-[12px] text-gray-500"><%= @minimum_password_length %> characters minimum</span>
          <% end %>
          <%= f.password_field :password, autocomplete: "new-password",
                class: "mt-1 block w-full rounded-lg border border-gray-300 px-3 py-2.5 text-[14px] text-gray-900 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
        </div>

        <div>
          <%= f.label :password_confirmation, "Confirm password*", class: "block text-[13px] font-medium text-gray-700" %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password",
                class: "mt-1 block w-full rounded-lg border border-gray-300 px-3 py-2.5 text-[14px] text-gray-900 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500" %>
        </div>

        <%= f.submit "Update password",
              class: "mt-2 inline-flex w-full items-center justify-center rounded-lg bg-[#4F5BFF] px-4 py-3 text-[14px] font-semibold text-white hover:bg-[#3f49ff] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#4F5BFF]" %>
      </div>
    <% end %>

    <div class="mt-6 text-center text-[13px] text-gray-600">
      <%= link_to "Back to login", new_session_path(resource_name), class: "font-medium text-indigo-600 hover:text-indigo-500" %>
    </div>
  </div>
</div>