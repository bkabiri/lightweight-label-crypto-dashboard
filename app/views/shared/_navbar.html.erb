<nav class="sticky top-0 z-40 bg-white/90 backdrop-blur border-b">
  <div class="mx-auto max-w-7xl px-6 py-3 flex items-center justify-between">
    <!-- Brand -->
    <%= link_to root_path, class: "flex items-center gap-2 font-semibold text-gray-900" do %>
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <circle cx="8" cy="12" r="4"></circle><circle cx="16" cy="12" r="4"></circle>
      </svg>
      <span class="text-lg">tokenlab</span>
    <% end %>

    <!-- Desktop menu -->
    <ul class="hidden md:flex items-center gap-6 text-sm text-gray-700">
      <li><a href="#" class="hover:text-gray-900">Solutions</a></li>
      <li><a href="#" class="hover:text-gray-900">Customers</a></li>
      <li><a href="#" class="hover:text-gray-900">Developers</a></li>
      <li><a href="#" class="hover:text-gray-900">Security</a></li>
      <li><a href="#" class="hover:text-gray-900">Company</a></li>
    </ul>

    <!-- Right (desktop) -->
    <% unless user_signed_in? %>
      <div class="hidden md:flex items-center gap-6">
        <%= link_to "Log In", new_user_session_path, class: "text-sm text-gray-700 hover:text-gray-900" %>
        <%= link_to "Get Started Now", new_user_registration_path, class: "inline-flex items-center rounded-xl bg-black px-4 py-2 text-white text-sm font-semibold shadow hover:shadow-md" %>
      </div>
    <% end %>

    <!-- Mobile toggle -->
    <label for="nav-toggle" class="md:hidden inline-flex items-center justify-center rounded-lg border px-2.5 py-2">
      <span class="sr-only">Open menu</span>
      <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor">
        <path stroke-linecap="round" stroke-width="2" d="M4 7h16M4 12h16M4 17h16"/>
      </svg>
    </label>
  </div>

  <!-- Mobile menu -->
  <input id="nav-toggle" type="checkbox" class="hidden peer">
  <div class="md:hidden peer-checked:block border-t">
    <ul class="px-6 py-4 space-y-2 text-sm">
      <li><a href="#" class="block py-2 text-gray-700">Solutions</a></li>
      <li><a href="#" class="block py-2 text-gray-700">Customers</a></li>
      <li><a href="#" class="block py-2 text-gray-700">Developers</a></li>
      <li><a href="#" class="block py-2 text-gray-700">Security</a></li>
      <li><a href="#" class="block py-2 text-gray-700">Company</a></li>
      <% unless user_signed_in? %>
        <li class="pt-2 flex items-center justify-between">
          <%= link_to "Log In", new_user_session_path, class: "text-gray-700" %>
          <%= link_to "Get Started Now", new_user_registration_path, class: "inline-flex items-center rounded-xl bg-black px-4 py-2 text-white font-semibold" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>